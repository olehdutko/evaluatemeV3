{
  "name": "@evaluateme/domain",
  "version": "1.0.0",
  "private": true,
  "description": "Pure TypeScript domain layer shared across apps",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint 'src/**/*.ts'",
    "test:integration": "echo 'No integration tests'",
    "test": "jest"
  },
  "devDependencies": {
    "@evaluateme/tsconfig": "*",
    "typescript": "^5.4.0",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0"
  }
}
